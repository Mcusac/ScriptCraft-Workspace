# constants.py
# System-level constants for the codebase
#
# Contains constants used across multiple modules for consistency and maintainability.

# Byte conversion constants
BYTES_PER_KB = 1024
BYTES_PER_MB = BYTES_PER_KB * 1024
BYTES_PER_GB = BYTES_PER_MB * 1024
